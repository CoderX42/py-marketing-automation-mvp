# macOS 首次使用（v1.1）

发行包支持 Apple Silicon 和 Intel Mac（通用版）。首次运行需要联网，用于安装桌面自动化所需的工具。

## 1. 解压并启动

1. 将压缩包解压到“下载”或“应用程序”以外的可写目录。
2. 双击 `首次运行.command`。
3. 如果 macOS 阻止脚本，右键文件选择“打开”；仍被拦截时到“系统设置 → 隐私与安全性”点击“仍要打开”。
4. 脚本会按需安装 Python、Java 17、Android Platform-Tools、Node.js、Appium 和 UiAutomator2。安装过程可能要求输入 Mac 登录密码。
5. 安装完成后会自动打开 `MarketingAutomation-v1.1.app`。以后可以直接双击应用；如果需要重新检查环境，再运行一次 `首次运行.command`。

## 2. 连接手机

1. 在安卓手机打开开发者选项和 USB 调试。
2. 用支持数据传输的 Type-C 数据线连接 Mac，解锁手机并在授权弹窗中点击“允许”。
3. 在桌面应用点击“检测设备”，看到设备状态为“已连接，USB 调试授权正常”。
4. 如果手机上还没有业务应用，在桌面应用选择 APK 后点击“安装 APK”。APK 文件需要由使用者另行提供。

## 3. 开始查询

1. 点击“下载 Excel 模板”，填写手机号后保存。
2. 选择手机号 Excel，确认手机上的业务应用已经登录并停在可操作页面。
3. 第一次运行保留“试运行 1 个号码”，确认手机完整执行“智慧营销 → 个人业务 → 全景视图 → 营销助手(免签入)”后，再取消勾选批量处理。
4. 新任务默认每条间隔 30 秒，并随机增加 0–5 秒；可在界面中调整。

## 4. 常见问题

- **找不到设备**：在终端执行 `adb devices`，确认状态为 `device`；若为 `unauthorized`，解锁手机并重新允许调试。
- **应用提示网络不可用**：保持稳定的业务网络。v1.1 会先清除顶部红色横幅、等待底部超时提示消失，再重试当前号码；持续失败会暂停并保存已采集内容，恢复网络或重新登录后在同一窗口点击“开始处理”。
- **直接双击应用后提示缺少 ADB/Appium**：关闭应用，运行 `首次运行.command` 完成环境安装。
- **每次开始处理都弹出 UiAutomator2 安装确认**：电脑上 npm 安装的 UiAutomator2 驱动不等于手机上的辅助 APK。首次请在手机上确认 `io.appium.uiautomator2.server` 和 `io.appium.uiautomator2.server.test` 两个安装弹窗，并保持手机解锁；两者都安装成功后，程序会自动检测并跳过重复安装。
- **Intel Mac 无法打开**：请确认使用发行包中的通用版应用；在“关于本机”查看芯片类型。通用版同时支持 Intel 和 Apple Silicon。

详细的 ADB 操作和网络排查步骤见 `ADB连接操作文档.md`。
