# 营销详情批量查询项目——会话上下文总结

- 整理日期：2026-09-13
- 项目目录：`/Users/karl/doc/study/odsidian-md/outputs/marketing-automation-mvp`
- 当前版本：`v1.1`
- 目标平台：Windows 10/11 x64、macOS

## 一、项目目标

制作一个桌面自动化工具，读取用户录入的手机号码 Excel，通过 ADB/Appium 控制安卓手机上的业务应用，逐条查询营销详情，并将每个号码对应的营销详情完整写入 Excel。

核心操作链路必须保持完整：

> 首页 → 智慧营销 → 个人业务 → 全景视图 → 营销助手（免签入） → 输入手机号码 → 跳转 → 获取营销详情页面全部内容 → 按当前手机号码写入 Excel

注意：

- 使用“营销助手（免签入）”入口。
- 不进入普通“营销助手”。
- 智慧营销页面暂无数据时，可从首页“常用”区域点击“营销助手（免签入）”继续。
- 营销详情页面内容可滚动，必须向下滚动并采集全部文本内容。
- 不需要保存截图，只保存页面文字和结构化结果。
- Excel 中每个能够进入营销详情页面的号码，都必须保存对应数据。
- 页面出现错误弹窗时，关闭弹窗并跳过当前号码，继续下一条。
- 网络异常或掉线时暂停任务，允许重新登录后继续，已处理号码自动跳过。
- 默认不要主动检测登录状态，避免误判已登录状态；只有实际操作失败时再提示用户处理。

## 二、功能要求

### Excel

- 提供手机号码 Excel 模板下载。
- 模板便于用户录入多条手机号码。
- 输出表格应方便查看，至少包含：手机号码、处理状态、营销详情全文、错误信息、处理时间。
- 查询时间不要包含字母 `T`，使用易读格式，例如 `2026-09-13 10:20:30`。
- 输出文件按当前查询号码关联保存，不能只保存最后一条结果。
- 支持打开结果 Excel。

### 执行控制

- 每条号码之间有基础间隔，默认值应偏慢，防止操作过快和触发限流。
- 支持随机增加间隔。
- 支持按批次暂停和设置暂停时长。
- 支持“先试运行 1 个号码”选项，确认手机端真实发生点击、进入正确页面并成功导出后，再进行批量处理。
- 网络异常时应自动重试同一号码，不能因一次网络提示直接丢失任务。
- 运行日志要显示当前进度、号码后四位、成功/空结果/失败/跳过数量和输出文件路径。

### 界面

- 版本号使用 `v1.0`、`v1.1` 格式。
- 减少标题和描述，不显示多余的“ANDROID WORKFLOW / DESKTOP”等大段文案。
- 修复过 UI 错位问题，采用更清晰的“数据与应用、设备连接、执行策略、运行日志”分区。
- 提供“操作指引 / 常见问题”入口。
- 提供“下载 Excel 模板”和“打开结果 Excel”按钮。
- 设备状态应清晰显示 ADB 连接和 USB 调试授权状态。

## 三、已遇到的问题及处理结论

### 1. macOS 启动问题

- `run_mac.command` 曾因 `/usr/local/Caskroom/android-platform-tools/*` 在 zsh 下无匹配文件而报错。
- 处理方向是避免未匹配 glob 直接展开，并使用实际存在的 Homebrew/ADB 路径。
- macOS 启动时还出现过 `urllib3 NotOpenSSLWarning`、缺少 `-apple-system` 字体别名和 IMK 日志。这些属于警告或系统日志，不是业务处理失败的主要原因。

### 2. Windows 环境检查问题

- Win10 电脑可能没有 WinGet，因此不能把 WinGet 作为唯一安装方式。
- 现在脚本支持官方下载回退，并将运行时放在：
  `%LOCALAPPDATA%\\MarketingAutomation\\runtime`
- Python、Node.js、Java 应优先复用已安装版本。

### 3. Android SDK 解压失败

原始错误：

```text
Cannot find path ...\\expanded\\cmdline-tools\\bin\\ because it does not exist
```

判断原因：Android 命令行工具 ZIP 中实际包含 `cmdline-tools/bin`，但 ZIP 内最长路径叠加原来的 GUID 临时目录后，路径接近或超过 Windows 10 传统 `MAX_PATH` 限制。PowerShell 解压失败后又被后续检查掩盖成“bin 不存在”。

已完成修复：

- 新增 `android_sdk_tools.py`，使用 Python 标准库 `zipfile` 解压。
- 使用短临时目录。
- Windows 路径使用 `\\?\\` 长路径形式。
- 解压完成后校验 `bin/sdkmanager.bat`、`lib/sdkmanager-classpath.jar`、`source.properties`。
- 失败时保留已有安装，不覆盖旧的可用目录。
- 校验 Android 工具官方 SHA1：
  `54a582f3bf73e04253602f2d1c80bd5868aac115`

### 4. 手机端没有真实点击

曾出现桌面日志显示任务开始，但手机页面仍停留首页。后续实现要求自动化必须逐步确认：

1. 点击底部“智慧营销”。
2. 点击“个人业务”。
3. 等待并点击“全景视图”。
4. 点击“营销助手（免签入）”。
5. 等待手机号码输入页加载完成。
6. 输入号码并点击跳转。
7. 等待营销详情页面加载。
8. 向下滚动，直到所有页面内容读取完成。
9. 提取文本并写入当前号码对应的 Excel 行。

如果坐标操作没有进入预期页面，应使用控件文本/页面 XML 定位，并记录当前 Activity 或页面标识。

### 5. 登录和网络掉线

- 应用登录状态有时会在退出旧窗口或运行一段时间后失效。
- 应用也会出现“当前网络不可用，请检查网络环境”或“网络连接超时，请稍后再试”。
- 该提示可能遮挡页面并阻止点击，导致抓取任务终止。
- 用户要求：出现网络异常时暂停，等待用户在手机恢复网络或重新登录，再点击继续；不要把已完成号码重复处理。
- 当前策略是不默认检测登录状态，避免误伤已经登录的会话；仅在实际入口/查询操作失败时处理异常。

## 四、当前代码和交付文件

项目目录：

`/Users/karl/doc/study/odsidian-md/outputs/marketing-automation-mvp`

主要文件：

- `app.py`：桌面界面和任务控制。
- `automation.py`：安卓页面自动化、导航、滚动和详情采集。
- `appium_server.py`：Appium 服务管理。
- `device.py`：ADB 设备检测、安装和启动应用。
- `excel_io.py`：手机号读取、结果写入和模板处理。
- `windows_bootstrap.ps1`：Windows 环境检查和安装。
- `android_sdk_tools.py`：Windows Android SDK 长路径解压工具。
- `setup_windows.ps1`：Windows 首次运行入口。
- `run_windows.bat`：Windows 双击启动脚本。
- `build_windows_portable.ps1`：Windows 便携版构建方案。
- `WINDOWS首次使用.md`：Windows 首次使用、ADB 和常见问题说明。
- `ADB连接操作文档.md`：安卓手机开启开发者选项、USB 调试和 ADB 授权步骤。
- `tests/test_android_sdk_tools.py`：SDK 解压相关回归测试。

## 五、Windows 环境包

### SDK 修复补丁

[windows-v1.1-sdk-fix-patch.zip](</Users/karl/doc/study/odsidian-md/outputs/marketing-automation-mvp/windows-v1.1-sdk-fix-patch.zip>)

适用于已有旧版本目录、但第 4 步出现 `expanded\\cmdline-tools\\bin` 错误的情况。覆盖：

- `windows_bootstrap.ps1`
- `android_sdk_tools.py`

### Windows 完整源码运行包

[marketing-automation-windows-v1.1-sdkfix.zip](</Users/karl/doc/study/odsidian-md/outputs/marketing-automation-mvp/marketing-automation-windows-v1.1-sdkfix.zip>)

### Windows 10 环境安装包

[marketing-automation-windows-v1.1-offline.zip](</Users/karl/doc/study/odsidian-md/outputs/marketing-automation-mvp/marketing-automation-windows-v1.1-offline.zip>)

该包约 543 MB，包含：

- Python 3.12.10 x64 安装包。
- Node.js 22.14.0 x64 MSI。
- Microsoft OpenJDK 17 x64 压缩包。
- Android Command-line Tools 19.0。
- Android Platform Tools / ADB。
- Appium 3.7.0 与 UiAutomator2 Driver 8.6.4 的 npm 缓存。
- `install_windows_env.bat` 和 `install_windows_env.ps1`。
- 应用源码和启动脚本。

使用方式：

1. 解压压缩包。
2. 进入 `windows-env-cache` 文件夹。
3. 双击 `install_windows_env.bat`。
4. 安装完成后返回上一级，双击 `run_windows.bat`。

环境包 SHA256：

```text
marketing-automation-windows-v1.1-offline.zip
628f09839efcd7ecc1ee7a25fc87adc11d28d6283264c05c7cdf7cacf54861e0
```

环境包内核心文件校验值记录在 `windows-env-cache/SHA256SUMS.txt`。

## 六、验证情况

已完成的针对性验证：

- `test_android_sdk_tools.py`：5 项测试全部通过。
- 覆盖深层路径、Unicode/空格路径、隐式目录、失败重试、旧安装保护、SHA1 错误和目录穿越防护。
- PowerShell 脚本语法解析通过。
- `git diff --check` 无空白错误。
- Windows 环境包 ZIP 完整性检查通过，12538 个条目无损坏。

尚未完成：

- 当前环境不是 Windows，未在用户实体 Win10 电脑上完成从安装到手机查询的全流程实测。
- 尚未交付真正离线免安装的 Windows EXE；当前环境包仍可能在 Appium 驱动或 Python 桌面依赖缓存不足时联网补齐。
- 手机厂商 USB 驱动可能需要单独安装或管理员授权。

## 七、后续建议

1. 用户先使用 Windows 环境包在 Win10 x64 上安装并运行环境检查。
2. 手机保持已登录状态，开启 USB 调试并允许电脑授权。
3. 先勾选“试运行 1 个号码”，确认手机真实完成完整入口链路和详情导出。
4. 确认单条结果无误后，再取消试运行并批量执行。
5. 若出现网络横幅，暂停任务，恢复网络/重新登录后继续，不清空已有输出文件。
6. 后续若需要真正双击 EXE 免安装，应在 Windows 机器上使用 `build_windows_portable.ps1` 构建并验证 PyInstaller 便携版。

## 八、用户明确提出的界面和文案偏好

- 中文界面。
- 版本号统一使用 `v1.0`、`v1.1`。
- 标题简洁，避免重复描述。
- 输出文件和查询时间易读。
- 操作指引和常见问题要能直接帮助普通用户排查设备、登录、网络和 Excel 问题。
- 用户希望减少反复测试，优先完成可下载、可安装、可运行的交付物。
